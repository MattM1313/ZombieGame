using UnityEngine;
using System.Collections;

public class boobies : MonoBehaviour {

	string titSize = "";

	//  this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		titSize += "D";
		Debug.Log("BOOBIES! Size: " + titSize);
	}
}
